// TitleScreenScene.js

import GameScene from "../Core/GameScene";

class TitleScreenScene extends GameScene {
	init() {
		const rootUI = AdvancedDynamicTexture.CreateFullscreenUI("rootUI", true, this._scene);

		// Game Logo
		
	}
}

export default TitleScreenScene;